import React, { useState } from 'react';

export const InteractiveUJM = () => {
  const [activeStage, setActiveStage] = useState(0);

  const stages = [
    {
      id: 0,
      label: "Preparation",
      icon: <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><path d="M9 18V5l12-2v13"></path><circle cx="6" cy="18" r="3"></circle><circle cx="18" cy="16" r="3"></circle></svg>,
      before: "Unmotivated to explore the massive, generic campus. Feels like a daunting chore rather than an adventure.",
      after: "Taps a mysterious '?' pin on the interactive map, instantly triggering curiosity and starting a gamified quest."
    },
    {
      id: 1,
      label: "Navigation",
      icon: <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><polygon points="3 11 22 2 13 21 11 13 3 11"></polygon></svg>,
      before: "GPS fails indoors; complex buildings (like CB) feel like a maze, leading to severe anxiety.",
      after: "Answers playful quizzes about XJTLU's philosophy to unlock step-by-step visual clues, turning navigation into a puzzle."
    },
    {
      id: 2,
      label: "Reception",
      icon: <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><rect x="2" y="3" width="20" height="14" rx="2" ry="2"></rect><line x1="8" y1="21" x2="16" y2="21"></line><line x1="12" y1="17" x2="12" y2="21"></line></svg>,
      before: "Official info plaques are text-heavy, dull, and non-interactive. Users quickly disengage.",
      after: "Taps physical NFC tag to summon a 3D AR mascot, revealing engaging lore in an immersive way."
    },
    {
      id: 3,
      label: "Sharing",
      icon: <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><path d="M23 19a2 2 0 0 1-2 2H3a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h4l2-3h6l2 3h4a2 2 0 0 1 2 2z"></path><circle cx="12" cy="13" r="4"></circle></svg>,
      before: "Exploring alone feels isolating. Taking photos of empty buildings feels uninspiring.",
      after: "Takes an AR selfie with the mascot and posts it to the 'Message Wall' to connect with others."
    },
    {
      id: 4,
      label: "Reflection",
      icon: <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"></path><polyline points="22 4 12 14.01 9 11.01"></polyline></svg>,
      before: "Leaves campus lacking a sense of achievement and fails to build community belonging.",
      after: "Claims the new badge in 'Collection', feeling proud, accomplished, and connected."
    }
  ];

  return (
    <div className="interactive-ujm-root">
      {/* 阶段导航条 */}
      <div className="interactive-ujm-stage-bar">
        {stages.map((s) => (
          <button
            key={s.id}
            onClick={() => setActiveStage(s.id)}
            className={`interactive-ujm-stage-btn${activeStage === s.id ? ' active' : ''}`}
          >
            {s.icon}
            {s.label}
          </button>
        ))}
      </div>

      {/* 内容展示区 */}
      <div className="interactive-ujm-content-grid">
        {/* Pain Point 卡片 */}
        <div className="interactive-ujm-card interactive-ujm-card-friction">
          <div className="interactive-ujm-card-title interactive-ujm-card-title-friction">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round"><circle cx="12" cy="12" r="10"></circle><line x1="12" y1="8" x2="12" y2="12"></line><line x1="12" y1="16" x2="12.01" y2="16"></line></svg>
            <span>Current Friction</span>
          </div>
          <p className="interactive-ujm-card-desc">
            {stages[activeStage].before}
          </p>
          <div className="interactive-ujm-card-watermark interactive-ujm-card-watermark-friction">
             <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2"><circle cx="12" cy="12" r="10"></circle><line x1="12" y1="8" x2="12" y2="12"></line><line x1="12" y1="16" x2="12.01" y2="16"></line></svg>
          </div>
        </div>

        {/* Campass Solution 卡片 */}
        <div className="interactive-ujm-card interactive-ujm-card-solution">
          <div className="interactive-ujm-card-title interactive-ujm-card-title-solution">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round"><path d="m12 3-1.912 5.813a2 2 0 0 1-1.275 1.275L3 12l5.813 1.912a2 2 0 0 1 1.275 1.275L12 21l1.912-5.813a2 2 0 0 1 1.275-1.275L21 12l-5.813-1.912a2 2 0 0 1-1.275-1.275L12 3Z"></path></svg>
            <span>Campass Intervention</span>
          </div>
          <p className="interactive-ujm-card-desc">
            {stages[activeStage].after}
          </p>
           <div className="interactive-ujm-card-watermark interactive-ujm-card-watermark-solution">
             <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2"><path d="m12 3-1.912 5.813a2 2 0 0 1-1.275 1.275L3 12l5.813 1.912a2 2 0 0 1 1.275 1.275L12 21l1.912-5.813a2 2 0 0 1 1.275-1.275L21 12l-5.813-1.912a2 2 0 0 1-1.275-1.275L12 3Z"></path></svg>
          </div>
        </div>
      </div>
    </div>
  );
};

# User Requirements

## User Journey Map: The "Solo Quest"

To deeply understand the friction points our target users face, we mapped the **current frustrating experience (Grey)** against the **enhanced, playful journey (Yellow)** enabled by Campass.

<div style={{ textAlign: 'center', margin: '2rem 0' }}>
  <img 
    src="/images/user-journey-map.png" 
    alt="Campass User Journey Map" 
    style={{ 
      borderRadius: 'var(--radius-card, 16px)', 
      width: '100%',
      maxWidth: '1000px',
      filter: 'drop-shadow(0 10px 30px rgba(40, 21, 89, 0.15))'
    }} 
  />
</div>

## Deep Dive into Experience Stages
*Select a stage below to explore the transformation in user experience.*

<InteractiveUJM />